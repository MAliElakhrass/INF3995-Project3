<?xml version="1.0" encoding="utf-8"?>
<android.support.constraint.ConstraintLayout
    android:id="@+id/activity_main_"
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/cafe_latte"
    tools:context=".Activities.MainActivity">

    <EditText
        android:id="@+id/textView_customer_identification"
        android:layout_width="250dp"
        android:layout_height="wrap_content"
        android:textColor="#000000"
        android:hint="Enter your username"
        android:gravity="center"
        android:padding="15dp"
        android:textSize="24dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintLeft_toLeftOf="@+id/activity_main_user_btn"
        app:layout_constraintRight_toRightOf="@+id/activity_main_user_btn"/>

    <Button
        android:id="@+id/activity_main_user_btn"
        android:layout_width="200dp"
        android:layout_height="wrap_content"
        app:layout_constraintTop_toBottomOf="@id/textView_customer_identification"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        android:background="@drawable/rounded_button"
        android:textColor="#000000"
        android:text="Start!"
        android:fontFamily="@font/freescpt"
        android:textSize="32dp"
        android:textStyle="bold"/>

    <Button
        android:id="@+id/activity_main_supervisor_btn"
        android:layout_height="wrap_content"
        android:layout_width="wrap_content"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        android:text="SUPERVISOR"
        android:textColor="#000000"/>

</android.support.constraint.ConstraintLayout>